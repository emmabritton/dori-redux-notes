plugins {
    id 'com.android.library'
    id 'org.jetbrains.kotlin.android'
}

android {
    namespace 'app.emmabritton.android_fsm'
    compileSdk compileVer

    defaultConfig {
        minSdk minVer
        targetSdk targetVer
    }
}

dependencies {
    implementation project(":fsm")
}